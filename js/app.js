const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav');if(toggle&&nav){toggle.addEventListener('click',()=>nav.classList.toggle('open'))}const weddingDate=new Date('2026-10-03T14:30:00+10:00');const countdown=document.getElementById('countdown');function tick(){if(!countdown)return;const diff=weddingDate-new Date();if(diff<=0){countdown.textContent='Today is the day!';return}const d=Math.floor(diff/86400000),h=Math.floor(diff/3600000)%24,m=Math.floor(diff/60000)%60;countdown.textContent=`${d} days · ${h} hours · ${m} minutes to go`;}tick();setInterval(tick,60000);const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('service-worker.js').catch(()=>{}));}
